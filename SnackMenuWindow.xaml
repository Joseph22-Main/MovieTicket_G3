<Window x:Class="Pre_Order_Menu.SnackMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pre_Order_Menu"
        mc:Ignorable="d"
        Title="Snack Menu" Height="500" Width="500" Background="#28282B">
    <Window.Resources>
        <!-- Circular Button Style -->
        <Style x:Key="CircularButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#444"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CircularTextBoxStyle" TargetType="TextBox">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#555"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                            CornerRadius="15"
                            SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PillButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                        CornerRadius="25"  
                            SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Snack Menu" FontSize="28" FontWeight="Bold"
           HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="White"/>

        <!-- Menu Items -->
        <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center">
            <!-- Popcorn -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="C:\Users\Lorenz School\source\repos\Pre-Order Menu\Images\pexels-alena-shekhovtcova-6941039.jpg" Width="50" Height="50" Margin="5"/>
                <TextBlock Text="Popcorn - ₱80" FontSize="18" Width="170" VerticalAlignment="Center" Foreground="White"/>
                <Button Content="-" Style="{StaticResource CircularButtonStyle}" Width="30" Click="DecreaseQuantity" Tag="popcorn"/>
                <TextBox x:Name="txtPopcorn" Style="{StaticResource CircularTextBoxStyle}" Text ="0" Margin="5,0"/>
                <Button Content="+" Style="{StaticResource CircularButtonStyle}" Width="30" Click="IncreaseQuantity" Tag="popcorn"/>
            </StackPanel>

            <!-- Soda -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="C:\Users\Lorenz School\source\repos\Pre-Order Menu\Images\pexels-hadis-7429792.jpg" Width="50" Height="50" Margin="5"/>
                <TextBlock Text="Soda - ₱50" FontSize="18" Width="170" VerticalAlignment="Center" Foreground="White"/>
                <Button Content="-" Style="{StaticResource CircularButtonStyle}" Width="30" Click="DecreaseQuantity" Tag="soda"/>
                <TextBox x:Name="txtSoda" Style="{StaticResource CircularTextBoxStyle}" Text ="0" Margin="5,0"/>
                <Button Content="+" Style="{StaticResource CircularButtonStyle}" Width="30" Click="IncreaseQuantity" Tag="soda"/>
            </StackPanel>

            <!-- Nachos -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="C:\Users\Lorenz School\source\repos\Pre-Order Menu\Images\pexels-karolina-grabowska-4964114.jpg" Width="50" Height="50" Margin="5"/>
                <TextBlock Text="Nachos - ₱100" FontSize="18" Width="170" VerticalAlignment="Center" Foreground="White"/>
                <Button Content="-" Style="{StaticResource CircularButtonStyle}" Width="30" Click="DecreaseQuantity" Tag="nachos"/>
                <TextBox x:Name="txtNachos" Style="{StaticResource CircularTextBoxStyle}" Text="0" Margin="5,0"/>
                <Button Content="+" Style="{StaticResource CircularButtonStyle}" Width="30" Click="IncreaseQuantity" Tag="nachos"/>
            </StackPanel>

            <!-- Hotdogs -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="C:\Users\Lorenz School\source\repos\Pre-Order Menu\Images\pexels-pixabay-33129.jpg" Width="50" Height="50" Margin="5"/>
                <TextBlock Text="Hotdogs - ₱70" FontSize="18" Width="170" VerticalAlignment="Center" Foreground="White"/>
                <Button Content="-" Style="{StaticResource CircularButtonStyle}" Width="30" Click="DecreaseQuantity" Tag="hotdogs"/>
                <TextBox x:Name="txtHotdogs" Style="{StaticResource CircularTextBoxStyle}" Text="0" Margin="5,0"/>
                <Button Content="+" Style="{StaticResource CircularButtonStyle}" Width="30" Click="IncreaseQuantity" Tag="hotdogs"/>
            </StackPanel>

            <!-- Fries -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="C:\Users\Lorenz School\source\repos\Pre-Order Menu\Images\pexels-polina-tankilevitch-4518643.jpg" Width="50" Height="50" Margin="5"/>
                <TextBlock Text="Fries - ₱60" FontSize="18" Width="170" VerticalAlignment="Center" Foreground="White"/>
                <Button Content="-" Style="{StaticResource CircularButtonStyle}" Width="30" Click="DecreaseQuantity" Tag="fries"/>
                <TextBox x:Name="txtFries" Style="{StaticResource CircularTextBoxStyle}" Text="0" Margin="5,0"/>
                <Button Content="+" Style="{StaticResource CircularButtonStyle}" Width="30" Click="IncreaseQuantity" Tag="fries"/>
            </StackPanel>
        </StackPanel>

        <!-- Bottom Buttons (Row 2) -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Add to Order"
            Style="{StaticResource PillButtonStyle}"
            Click="AddToOrder_Click"
            Margin="0,0,10,0"/>

            <Button Content="Show List"
            Style="{StaticResource PillButtonStyle}"
            Click="ShowList_Click"/>
        </StackPanel>
    </Grid>
</Window>
